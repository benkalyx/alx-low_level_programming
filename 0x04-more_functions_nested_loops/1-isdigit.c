#include "main.h"
/**
* _isdigit - a function to check for digit
*
*@c: integer passed as argument
*
* Return: 1 if its a digit, otherwise return 0
*/
int _isdigit(int c)
{
	if
	(c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
