#include "main.h"
/**
* mul - A function to multiply two integers
* @a: first integer passed as argument to the function
* @b: second integer passed as argument to the function
*
* Return: 0 (Success)
*/
int mul(int a, int b)
{
	int c;

	c = (a * b);
	return (c);
}
