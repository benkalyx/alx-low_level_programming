#include "main.h"
/**
* _islower - A program to check for lowercase character
*
* @c: is ASCII value
*
* Return: return 1 if the character is lower and 0 if otherwise
*/
int _islower(int c)
{
	if
	(c >=  97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
