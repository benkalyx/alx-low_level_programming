#include "main.h"
/**
* _abs - function defined to compute absolute value of an integer
*
* @n: shows that only integer values are accepted as argument
*
* Return: 0 (Success)
*/
int _abs(int n)
{
	if
	(n >= 0)
	{
		return (n);
	}
	else
	{
		return ((-1) * n);
	}
}
